# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Reduced memory for 3GB RAM system
org.gradle.jvmargs=-Xmx1536m -Xms256m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8

# Use in-process compilation to save memory by not starting a separate Kotlin daemon
kotlin.compiler.execution.strategy=in-process

# Limit workers to 1 to reduce memory pressure on low RAM systems
org.gradle.workers.max=1

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. For more details, visit
# https://developer.android.com/r/tools/gradle-multi-project-decoupled-projects
# org.gradle.parallel=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

android.useAndroidX=true
android.enableJetifier=false

# Edu_AI Global Access Configuration
INTERNAL_API_KEY=64923e4d8f1a2c5b9e0f3d7a6c5b9eX0f3d7a6c5b9e0f3d7a

# ? Hugging Face Production Backend:
backendBaseUrl=https://agent606-edu-ai.hf.space/
android.defaults.buildfeatures.resvalues=true
android.sdk.defaultTargetSdkToCompileSdkIfUnset=false
android.enableAppCompileTimeRClass=false
android.usesSdkInManifest.disallowed=false
android.uniquePackageNames=false
android.dependency.useConstraints=true
android.r8.strictFullModeForKeepRules=false
android.r8.optimizedResourceShrinking=false
android.builtInKotlin=false
android.newDsl=false
